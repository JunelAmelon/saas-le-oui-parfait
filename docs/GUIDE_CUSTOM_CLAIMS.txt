═══════════════════════════════════════════════════════════════════════════════
   🔐 GUIDE : AJOUTER CUSTOM CLAIMS { "role": "client" }
   Comment définir le rôle d'un utilisateur dans Firebase Auth
═══════════════════════════════════════════════════════════════════════════════

Les Custom Claims permettent d'ajouter des métadonnées personnalisées à un compte
Firebase Auth (comme le rôle : client, planner, admin, etc.)


═══════════════════════════════════════════════════════════════════════════════
MÉTHODE 1 : VIA FIREBASE CONSOLE (INTERFACE WEB) ✅ RECOMMANDÉ
═══════════════════════════════════════════════════════════════════════════════

ÉTAPE 1 : Aller dans Firebase Console
─────────────────────────────────────────────────────────────────────────────
1. Ouvrir : https://console.firebase.google.com/
2. Sélectionner votre projet : "saas-le-oui-parfait"
3. Dans le menu de gauche, cliquer sur "Authentication"
4. Onglet "Users"


ÉTAPE 2 : Trouver l'utilisateur
─────────────────────────────────────────────────────────────────────────────
1. Dans la liste des utilisateurs, trouver celui que vous voulez modifier
   Exemple : julie.martin@example.com
2. Cliquer sur la ligne de l'utilisateur


ÉTAPE 3 : Ajouter le Custom Claim
─────────────────────────────────────────────────────────────────────────────
1. Dans la page de détails de l'utilisateur, chercher la section "Custom claims"
2. Cliquer sur "Edit" ou le bouton "Set custom user claims"
3. Dans le champ JSON, entrer :

   {
     "role": "client"
   }

4. Cliquer "Save" ou "Enregistrer"


ÉTAPE 4 : Vérifier
─────────────────────────────────────────────────────────────────────────────
1. Recharger la page
2. La section "Custom claims" devrait afficher : { "role": "client" }
3. ✅ C'est fait !


⚠️ IMPORTANT : L'utilisateur doit se déconnecter et se reconnecter pour que
le custom claim soit pris en compte dans son token.


═══════════════════════════════════════════════════════════════════════════════
MÉTHODE 2 : VIA FIREBASE CLI (LIGNE DE COMMANDE)
═══════════════════════════════════════════════════════════════════════════════

PRÉREQUIS
─────────────────────────────────────────────────────────────────────────────
1. Installer Firebase CLI :
   npm install -g firebase-tools

2. Se connecter :
   firebase login


COMMANDE
─────────────────────────────────────────────────────────────────────────────
firebase auth:import users.json --hash-algo=scrypt

Ou pour un utilisateur spécifique, créer un script Node.js (voir Méthode 3)


═══════════════════════════════════════════════════════════════════════════════
MÉTHODE 3 : VIA SCRIPT NODE.JS (ADMIN SDK)
═══════════════════════════════════════════════════════════════════════════════

ÉTAPE 1 : Installer Firebase Admin SDK
─────────────────────────────────────────────────────────────────────────────
npm install firebase-admin


ÉTAPE 2 : Créer un script
─────────────────────────────────────────────────────────────────────────────
Créer : scripts/set-custom-claims.js

const admin = require('firebase-admin');

// Initialiser avec votre service account
const serviceAccount = require('./serviceAccountKey.json');

admin.initializeApp({
  credential: admin.credential.cert(serviceAccount)
});

// UID de l'utilisateur à modifier
const uid = 'abc123xyz456'; // ← Remplacer par l'UID réel

// Définir le custom claim
admin.auth().setCustomUserClaims(uid, { role: 'client' })
  .then(() => {
    console.log('✅ Custom claim ajouté avec succès !');
    console.log(`   UID: ${uid}`);
    console.log('   Claim: { "role": "client" }');
    process.exit(0);
  })
  .catch((error) => {
    console.error('❌ Erreur:', error);
    process.exit(1);
  });


ÉTAPE 3 : Obtenir le Service Account Key
─────────────────────────────────────────────────────────────────────────────
1. Firebase Console > Project Settings (⚙️)
2. Onglet "Service accounts"
3. Cliquer "Generate new private key"
4. Télécharger le fichier JSON
5. Renommer en "serviceAccountKey.json"
6. Placer dans le dossier scripts/


ÉTAPE 4 : Exécuter le script
─────────────────────────────────────────────────────────────────────────────
node scripts/set-custom-claims.js


═══════════════════════════════════════════════════════════════════════════════
MÉTHODE 4 : VIA CLOUD FUNCTIONS (AUTOMATIQUE)
═══════════════════════════════════════════════════════════════════════════════

Pour définir automatiquement le rôle lors de la création d'un compte :

// functions/index.js
const functions = require('firebase-functions');
const admin = require('firebase-admin');
admin.initializeApp();

exports.setUserRole = functions.auth.user().onCreate(async (user) => {
  // Définir le rôle par défaut
  const role = user.email.includes('@planner.') ? 'planner' : 'client';
  
  await admin.auth().setCustomUserClaims(user.uid, { role });
  
  console.log(`Role "${role}" défini pour ${user.email}`);
});


═══════════════════════════════════════════════════════════════════════════════
VÉRIFIER QUE LE CUSTOM CLAIM EST BIEN DÉFINI
═══════════════════════════════════════════════════════════════════════════════

MÉTHODE 1 : Via Firebase Console
─────────────────────────────────────────────────────────────────────────────
1. Authentication > Users
2. Cliquer sur l'utilisateur
3. Vérifier la section "Custom claims"


MÉTHODE 2 : Via le code (côté client)
─────────────────────────────────────────────────────────────────────────────
Dans votre application, après connexion :

import { getAuth } from 'firebase/auth';

const auth = getAuth();
const user = auth.currentUser;

if (user) {
  user.getIdTokenResult()
    .then((idTokenResult) => {
      console.log('Custom claims:', idTokenResult.claims);
      console.log('Role:', idTokenResult.claims.role);
    });
}


MÉTHODE 3 : Via un script de test
─────────────────────────────────────────────────────────────────────────────
Créer : scripts/check-claims.js

const admin = require('firebase-admin');
const serviceAccount = require('./serviceAccountKey.json');

admin.initializeApp({
  credential: admin.credential.cert(serviceAccount)
});

const uid = 'abc123xyz456'; // ← UID à vérifier

admin.auth().getUser(uid)
  .then((userRecord) => {
    console.log('Custom claims:', userRecord.customClaims);
  })
  .catch((error) => {
    console.error('Erreur:', error);
  });


═══════════════════════════════════════════════════════════════════════════════
DIFFÉRENTS RÔLES POSSIBLES
═══════════════════════════════════════════════════════════════════════════════

Pour votre application, vous pouvez définir :

{ "role": "client" }    → Client qui accède à l'espace client
{ "role": "planner" }   → Wedding planner qui gère l'agence
{ "role": "admin" }     → Administrateur système
{ "role": "vendor" }    → Prestataire (si vous voulez leur donner accès)


PLUSIEURS CLAIMS POSSIBLES
─────────────────────────────────────────────────────────────────────────────
Vous pouvez aussi ajouter d'autres informations :

{
  "role": "client",
  "premium": true,
  "agencyId": "agency-123"
}


═══════════════════════════════════════════════════════════════════════════════
UTILISATION DANS LES RÈGLES FIRESTORE
═══════════════════════════════════════════════════════════════════════════════

Une fois le custom claim défini, vous pouvez l'utiliser dans vos règles :

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Seuls les planners peuvent écrire
    match /clients/{clientId} {
      allow read: if request.auth != null;
      allow write: if request.auth != null && 
        request.auth.token.role == 'planner';
    }
    
    // Seuls les clients peuvent lire leurs propres données
    match /events/{eventId} {
      allow read: if request.auth != null && 
        (request.auth.token.role == 'client' || 
         request.auth.token.role == 'planner');
    }
  }
}


═══════════════════════════════════════════════════════════════════════════════
UTILISATION DANS VOTRE APPLICATION
═══════════════════════════════════════════════════════════════════════════════

Dans votre code React/Next.js :

// contexts/AuthContext.tsx
const user = auth.currentUser;
const idTokenResult = await user.getIdTokenResult();
const role = idTokenResult.claims.role;

// Redirection selon le rôle
if (role === 'client') {
  router.push('/espace-client');
} else if (role === 'planner') {
  router.push('/agence');
}


═══════════════════════════════════════════════════════════════════════════════
ERREURS COURANTES
═══════════════════════════════════════════════════════════════════════════════

ERREUR 1 : "Custom claims not found"
─────────────────────────────────────────────────────────────────────────────
Cause : L'utilisateur ne s'est pas reconnecté après l'ajout du claim
Solution : Se déconnecter et se reconnecter


ERREUR 2 : "Permission denied"
─────────────────────────────────────────────────────────────────────────────
Cause : Le custom claim n'est pas encore synchronisé
Solution : Attendre quelques secondes et rafraîchir le token :
  await user.getIdToken(true); // Force refresh


ERREUR 3 : "Invalid JSON"
─────────────────────────────────────────────────────────────────────────────
Cause : Syntaxe JSON incorrecte dans la console
Solution : Vérifier les guillemets doubles et la virgule finale


═══════════════════════════════════════════════════════════════════════════════
RÉSUMÉ RAPIDE
═══════════════════════════════════════════════════════════════════════════════

POUR AJOUTER { "role": "client" } :

1. Firebase Console > Authentication > Users
2. Cliquer sur l'utilisateur
3. Section "Custom claims" > Edit
4. Entrer : { "role": "client" }
5. Save
6. L'utilisateur doit se reconnecter

C'est tout ! ✅


═══════════════════════════════════════════════════════════════════════════════
EXEMPLE COMPLET POUR VOTRE CAS
═══════════════════════════════════════════════════════════════════════════════

1. Créer le compte :
   Email: julie.martin@example.com
   Password: TestClient123

2. Copier l'UID généré : "xYz789AbC123"

3. Ajouter custom claim :
   { "role": "client" }

4. Dans Firestore > clients :
   {
     "client_user_id": "xYz789AbC123",
     "name": "Julie Martin",
     ...
   }

5. Se connecter avec julie.martin@example.com
   → Redirigé vers /espace-client ✅


═══════════════════════════════════════════════════════════════════════════════
FIN DU GUIDE
═══════════════════════════════════════════════════════════════════════════════
