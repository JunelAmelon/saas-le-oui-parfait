═══════════════════════════════════════════════════════════════════════════════
   📊 ANALYSE DES COLLECTIONS EXISTANTES VS BESOINS ESPACE CLIENT
═══════════════════════════════════════════════════════════════════════════════

COLLECTIONS EXISTANTES DANS VOTRE FIRESTORE:
─────────────────────────────────────────────────────────────────────────────
✅ agency
✅ articles
✅ clients
✅ compositions
✅ contracts
✅ devis
✅ documents
✅ events
✅ fournisseurs
✅ inventaires
✅ invoices
✅ notes
✅ post_its
✅ profiles
✅ prospects
✅ tasks
✅ vendors

COLLECTIONS NÉCESSAIRES POUR L'ESPACE CLIENT:
─────────────────────────────────────────────────────────────────────────────
1. ✅ clients          → EXISTE DÉJÀ
2. ✅ events           → EXISTE DÉJÀ
3. ✅ documents        → EXISTE DÉJÀ
4. ❌ checklist        → MANQUANTE (peut utiliser "tasks" ?)
5. ❌ payments         → MANQUANTE (à créer ou utiliser invoices ?)
6. ✅ invoices         → EXISTE DÉJÀ
7. ✅ vendors          → EXISTE DÉJÀ
8. ❌ conversations    → MANQUANTE (pour messagerie)
9. ❌ messages         → MANQUANTE (pour messagerie)
10. ❌ galleries       → MANQUANTE (pour photos)


═══════════════════════════════════════════════════════════════════════════════
MAPPING RECOMMANDÉ
═══════════════════════════════════════════════════════════════════════════════

COLLECTION "clients" ✅
─────────────────────────────────────────────────────────────────────────────
Champs nécessaires pour l'espace client:
  - id
  - planner_id (ou agency_id)
  - client_user_id (UID Firebase Auth du client)
  - name
  - partner
  - email
  - phone
  - created_at

À VÉRIFIER: Est-ce que vos clients ont déjà un champ "client_user_id" ?


COLLECTION "events" ✅
─────────────────────────────────────────────────────────────────────────────
Champs nécessaires:
  - id
  - client_id (lien vers clients)
  - planner_id
  - couple_names
  - event_date
  - location
  - guest_count
  - budget
  - theme { style, colors[], description }
  - notes

À COMPLÉTER: Ajouter "theme" si manquant


COLLECTION "documents" ✅
─────────────────────────────────────────────────────────────────────────────
Champs nécessaires:
  - id
  - client_id
  - planner_id
  - name
  - type (contrat/devis/facture/photo/autre)
  - file_url
  - file_size
  - status (signed/accepted/paid/pending)
  - date ou uploaded_at

À VÉRIFIER: Structure actuelle de vos documents


COLLECTION "tasks" ✅ (peut servir de checklist)
─────────────────────────────────────────────────────────────────────────────
Peut être utilisée pour la checklist si elle contient:
  - id
  - event_id
  - client_id
  - title
  - description
  - deadline
  - completed (boolean)
  - category
  - priority

ALTERNATIVE: Créer une collection "checklist" séparée


COLLECTION "invoices" ✅ (peut servir pour payments)
─────────────────────────────────────────────────────────────────────────────
Champs nécessaires:
  - id
  - client_id
  - amount
  - status (paid/pending/overdue)
  - due_date
  - paid_date
  - description
  - vendor

RECOMMANDATION: Créer une collection "payments" séparée pour l'historique


COLLECTION "vendors" ✅
─────────────────────────────────────────────────────────────────────────────
Champs nécessaires:
  - id
  - name
  - category
  - contact
  - phone
  - email
  - address
  - website
  - status
  - rating
  - event_ids[] (liste des events associés)
  - next_appointment { date, time, description }

À COMPLÉTER: Ajouter "event_ids" et "next_appointment" si manquants


═══════════════════════════════════════════════════════════════════════════════
COLLECTIONS À CRÉER
═══════════════════════════════════════════════════════════════════════════════

1. PAYMENTS (recommandé)
─────────────────────────────────────────────────────────────────────────────
Pour l'historique des paiements distinct des factures:
{
  id: string,
  client_id: string,
  invoice_id: string,
  description: string,
  vendor: string,
  amount: number,
  status: 'paid' | 'pending' | 'overdue',
  method: string,
  date: string,
  due_date: string
}

2. CONVERSATIONS (pour messagerie temps réel)
─────────────────────────────────────────────────────────────────────────────
{
  id: string,
  client_id: string,
  planner_id: string,
  participants: string[],
  last_message: string,
  last_message_at: timestamp,
  unread_count_client: number,
  unread_count_planner: number
}

3. MESSAGES (pour messagerie temps réel)
─────────────────────────────────────────────────────────────────────────────
{
  id: string,
  conversation_id: string,
  sender_id: string,
  sender_role: 'client' | 'planner',
  content: string,
  attachments: string[],
  read: boolean,
  created_at: timestamp
}

4. GALLERIES (pour photos événement)
─────────────────────────────────────────────────────────────────────────────
{
  id: string,
  event_id: string,
  name: string,
  photos: [{
    id: string,
    url: string,
    uploaded_by: 'client' | 'planner',
    liked: boolean,
    uploaded_at: timestamp
  }]
}


═══════════════════════════════════════════════════════════════════════════════
RECOMMANDATIONS
═══════════════════════════════════════════════════════════════════════════════

OPTION 1: UTILISER LES COLLECTIONS EXISTANTES (RAPIDE)
─────────────────────────────────────────────────────────────────────────────
✅ Utiliser "clients" tel quel (ajouter client_user_id si manquant)
✅ Utiliser "events" (ajouter theme si manquant)
✅ Utiliser "documents" tel quel
✅ Utiliser "tasks" comme checklist
✅ Utiliser "invoices" pour les paiements
✅ Utiliser "vendors" (ajouter event_ids si manquant)
❌ Messagerie et Galerie non disponibles (à implémenter plus tard)

AVANTAGES:
- Pas besoin de créer de nouvelles collections
- Utilise vos données existantes
- Rapide à tester

INCONVÉNIENTS:
- Pas de messagerie temps réel
- Pas de galerie photos
- Moins de séparation entre factures et paiements


OPTION 2: CRÉER LES COLLECTIONS MANQUANTES (COMPLET)
─────────────────────────────────────────────────────────────────────────────
✅ Garder toutes vos collections existantes
➕ Créer "payments" pour historique distinct
➕ Créer "conversations" + "messages" pour messagerie
➕ Créer "galleries" pour photos

AVANTAGES:
- Fonctionnalités complètes
- Messagerie temps réel
- Galerie photos
- Meilleure organisation des données

INCONVÉNIENTS:
- Plus de collections à gérer
- Nécessite plus de configuration


═══════════════════════════════════════════════════════════════════════════════
DÉCISION RECOMMANDÉE
═══════════════════════════════════════════════════════════════════════════════

👉 OPTION 1 POUR COMMENCER (tester rapidement)

Puis ajouter progressivement:
1. Collection "payments" (améliore la gestion budgétaire)
2. Collections "conversations" + "messages" (messagerie)
3. Collection "galleries" (photos)


═══════════════════════════════════════════════════════════════════════════════
PROCHAINE ÉTAPE
═══════════════════════════════════════════════════════════════════════════════

Je vais créer un seeder qui:
1. Utilise vos collections EXISTANTES (clients, events, documents, tasks, invoices, vendors)
2. Ajoute les champs manquants si nécessaire
3. Crée des données de test pour un client
4. Permet de tester l'espace client immédiatement

Voulez-vous que je procède avec cette approche ?
